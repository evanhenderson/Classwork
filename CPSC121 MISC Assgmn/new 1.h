#include <iostream>
using namespace std;
bool linearSearch(int value[], int size, int num)
{
	for (int i = 0; i < size, i++)
	{
		if value[i] == num)
		{
			return true;
		}
	}
	return false;
}